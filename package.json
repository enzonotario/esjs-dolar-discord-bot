{
  "name": "@es-js/crear-discord-bot",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch 'bot/**/*' -e esjs,js,ts,json --exec 'npm run build && npm run start'",
    "build": "vite build --ssr --mode=production",
    "serve": "vite preview",
    "start": "node dist/index.js",
    "deploy": "node dist/deploy.js"
  },
  "devDependencies": {
    "@es-js/vite-plugin-esjs": "^0.0.15",
    "nodemon": "^3.0.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "discord-api-types": "^0.37.66",
    "discord.js": "^14.14.1"
  }
}
